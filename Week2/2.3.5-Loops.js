// Follow the steps below to create a
//  **while** loop that TOTALS the numbers from 1 to 100

let x = 1;


// 1) create a variable to represent the current number
let total = 0
// 2) create a variable to represent the current total

// 3) write a while loop that sums the numbers from 1 to 100
while (x <= 100) {
  total = toatl + x;
  console.log(`x: ${x} tpta: ${total}`)
  x++;
}

// Now using the same variables you created in steps 1 and 2,
// create a **for** loops that TOTALS the numbers from 1 to 100

// 1) write a for loop that sums the numbers from 1 to 100
let total = 0;
for (let x = 1; x <= 100; x++) {
  total += x;
}

let x = 1;
for (let x = 1; <= 10; x++) {
  total + = x;
}


// this is a homework questions:
// count even numbers using modules(remainders) or divisiable

for (let i = 1; i <= 10;) {
  if (i % 5 === 0) {
    console.log(i)
  }
}
